<% if post.like_user(current_user.id) %>
    <%= button_to tweet_like_path(like, post_id: post.id), method: :delete, id: "like-button", remote: true do %>
      <%= image_tag("icon_red_heart.svg") %>
      <span>
        <%= post.like_count %>
      </span>
    <% end %>
<% else %>
    <%= button_to post_likes_path(post), id: "like-button", remote: true do %>
      <%= image_tag("icon_heart.svg") %>
      <span>
        <%= post.like_count %>
      </span>
    <% end %>
<% end %>
    <% = image_tag("icon_heart.svg") %>
        <span>
          <%= post.like_count %>
        </span>
    <% end %>