<%= form_with model: current_user, url: follow_relationships_path,method: :post, remote: true do |f| %>
  <%= f.hidden_field :following_id, value: @user.id %>
  <%= f.submit "フォロー", class: "btn btn-outline-secondary" %>
<% end %>