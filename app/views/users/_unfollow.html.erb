<%= form_with(model: current_user,url: following_relationship(@user).find_by(following_id: @user.id),method: :delete,remote: true) do |f| %>
  <%= f.hidden_field :following_id %>  
  <%= f.submit "フォロー解除", class: "btn btn-outline-secondary" %>
<% end %>